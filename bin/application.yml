server:
    port: 8888
spring:
    cloud:
        config:
            server:
                git:
                    uri: 'https://github.gapinc.com/plan/config_repo'
                    search-paths:
                        - vmi_poc_configclient
